
* The timestamps for the server -v output are currently in a random
  timezone. We need to reset the TZ before printing them.
* Add a way for "results" (e.g. bindists) to be uploaded
* Add SSL instructions to docs
* Work out if SSL is actually being used correctly
* Resource limits
* Protocol version
* Some sort of locking so the same client can't connect twice
* More exception handling
* "validate this bundle please" feature
* Add a "start build now" button to the web interface
* Add a --do-build-now client commandline flag
* e-mail report collation
* IRC reporting
* twitter
* RSS

